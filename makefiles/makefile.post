
CLEANFILES += $(DEBUG_OBJDIR)/*.d $(RELEASE_OBJDIR)/*.d

.PHONY: all default-build debug release install uninstall clean

ifdef DEBUG
default-build: debug
else
default-build: release
endif

clean:
	-rm -f $(CLEANFILES)

debug: $(DEBUG_TARGETS)

release: $(RELEASE_TARGETS)

install: $(INSTALLEDLIBS) $(INSTALLEDHEADERS) $(INSTALLEDPKGCONFIG) $(INSTALLEDSHAREFILES) $(INSTALLEDBINARIES) $(NONINSTALLEDBINARIES)

uninstall:
	$(SUDO) rm -fR $(UNINSTALLFILES)
