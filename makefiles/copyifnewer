#!/bin/sh
SRCFILE="$1"
DSTFILE="$2"
if [ -d "$SRCFILE" ] ; then
	install -o root -d "$DSTFILE"
else
	DSTDIR="`dirname "$DSTFILE"`"
	install -v -o root -d "$DSTDIR"
	install -C -v -o root "$SRCFILE" "$DSTFILE"
fi
