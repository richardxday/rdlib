#!/bin/sh
SRCFILE="$1"
DSTFILE="$2"
test "`uname -o`" == "Cygwin" && USER=richard || USER=root
if [ -d "$SRCFILE" ] ; then
	install -o $USER -d "$DSTFILE"
else
	DSTDIR="`dirname "$DSTFILE"`"
	install -v -o $USER -d "$DSTDIR"
	install -C -v -o $USER "$SRCFILE" "$DSTFILE"
fi
